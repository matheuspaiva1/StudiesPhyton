numeros = [int(num) for num in input().split()]

fat = numeros[1:-1]
print(fat)
'''
Implemente um programa que irá receber uma lista de inteiros. Obrigatoriamente armazene esses números numa lista.

Usando fatiamento (não pode usar for e nem while), gere uma nova lista com todos os elementos da lista de entrada, começando do primeiro índice até o ultimo índice.
'''